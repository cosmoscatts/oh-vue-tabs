export const TagWidgetBtnCloseProps = {
  isActive: {
    type: Boolean,
    required: true,
  },
  defaultColor: {
    type: String,
    required: false,
  },
  activeColor: {
    type: String,
    required: false,
  },
} as const
